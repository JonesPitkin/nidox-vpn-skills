# Podkop

Этот каталог зарезервирован для группы skills по `Podkop` внутри мета-репозитория `nidox-vpn-skills`.

Если материалы копируются или оформляются здесь, они автоматически подпадают под обязательный аудит через `nidox-vpn-detection-defense-skill`.

Самостоятельный исходный репозиторий:

- `podkop-skills`
