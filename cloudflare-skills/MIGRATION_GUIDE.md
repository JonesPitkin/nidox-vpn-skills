# Migration Guide

## From `nidox-vpn-skills/skills/cloudflare`

The legacy `skills/cloudflare/` directory in `nidox-vpn-skills` remains a compatibility marker. The standalone source of truth is this repository.

## Recommended Migration

1. maintain Cloudflare VPN publication knowledge here
2. sync a full snapshot into `nidox-vpn-skills/cloudflare-skills/`
3. keep the legacy thin entrypoint until downstream references are updated
